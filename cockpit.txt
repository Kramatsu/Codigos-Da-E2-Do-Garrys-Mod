@inputs A:wirelink B:entity Velocidade


if (first() / duped())
{
    runOnTick(1)
    A:egpClear()

    function void faroisLigados()
    {
        A:egpLine(1, vec2(200, 200), vec2(225, 200))
        A:egpLine(2, vec2(200, 207), vec2(225, 207))
        A:egpLine(3, vec2(200, 214), vec2(225, 214))
        A:egpCircle(4, vec2(240, 205), vec2(10, 20))
        A:egpSize(1, 3)
        A:egpSize(2, 3)
        A:egpSize(3, 3)
    }
    faroisLigados()
}
